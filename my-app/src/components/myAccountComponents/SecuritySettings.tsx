import { Pages } from './data/Pages';

interface switchToPageProps {
    switchToPage: (page: Pages) => void;
  };

export default function SecuritySettings({switchToPage}: switchToPageProps) {
    return (
        <div>

        </div>
    )
};